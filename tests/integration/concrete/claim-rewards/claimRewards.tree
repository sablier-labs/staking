ClaimRewards_Integration_Concrete_Test
├── when delegate call
│  └── it should revert
└── when not delegate call
   ├── when null
   │  └── it should revert
   └── when not null
      ├── when min fee not paid
      │  └── it should revert
      └── when min fee paid
         ├── when fee on rewards too high
         │  └── it should revert
         └── when fee on rewards not too high
            ├── given claimable rewards zero
            │  └── it should revert
            └── given claimable rewards not zero
                ├── given status SCHEDULED
                │  └── it should claim rewards
                ├── given status ENDED
                │  └── it should claim rewards
                └── given status ACTIVE
                   ├── it should emit 1 {SnapshotRewards}, 2 {Transfer} and 1 {ClaimRewards} events
                   ├── it should set rewards to zero
                   ├── it should transfer the min fee to comptroller
                   ├── it should transfer the rewards to the caller
                   └── it should return the rewards
