Getters_Integration_Concrete_Test::getAdmin
├── when null
│  └── it should revert
└── when not null
   └── it should return the admin

Getters_Integration_Concrete_Test::getCumulativeRewardAmount
├── when null
│  └── it should revert
└── when not null
   └── it should return the cumulative reward amount

Getters_Integration_Concrete_Test::getEndTime
├── when null
│  └── it should revert
└── when not null
   └── it should return the end time

Getters_Integration_Concrete_Test::getRewardAmount
├── when null
│  └── it should revert
└── when not null
   └── it should return the reward amount

Getters_Integration_Concrete_Test::getRewardToken
├── when null
│  └── it should revert
└── when not null
   └── it should return the reward token

Getters_Integration_Concrete_Test::getStakingToken
├── when null
│  └── it should revert
└── when not null
   └── it should return the staking token

Getters_Integration_Concrete_Test::getStartTime
├── when null
│  └── it should revert
└── when not null
   └── it should return the start time

Getters_Integration_Concrete_Test::getTotalStakedAmount
├── when null
│  └── it should revert
└── when not null
   └── it should return the total staked amount

Getters_Integration_Concrete_Test::globalRptScaledAtSnapshot
├── when null
│  └── it should revert
└── when not null
   ├── when start time in future
   │  └── it should return correct return values
   ├── when start time in present
   │  └── it should return correct return values
   └── when start time in past
      ├── when end time in future
      │  └── it should return correct return values
      └── when end time not in future
         └── it should return correct return values

Getters_Integration_Concrete_Test::isLockupWhitelisted
├── when zero address
│  └── it should revert
└── when not zero address
   ├── given not whitelisted
   │  └── it should return false
   └── given whitelisted
      └── it should return true

Getters_Integration_Concrete_Test::status
├── when null
│  └── it should revert
└── when not null
   ├── when start time in future
   │  └── it should return SCHEDULED
   └── when start time not in future
      ├── when end time in past
      │  └── it should return ENDED
      └── when end time not in past
         └── it should return ACTIVE

Getters_Integration_Concrete_Test::streamLookup
├── when zero address
│  └── it should revert
└── when not zero address
   ├── when stream not staked
   │  └── it should revert
   └── when stream staked
      └── it should return pool id and owner

Getters_Integration_Concrete_Test::totalAmountStakedByUser
├── when null
│  └── it should revert
└── when not null
   ├── when zero address
   │  └── it should revert
   └── when not zero address
      └── it should return the total amount staked by user

Getters_Integration_Concrete_Test::userAccount
├── when null
│  └── it should revert
└── when not null
   ├── when zero address
   │  └── it should revert
   └── when not zero address
      └── it should return correct user account details
