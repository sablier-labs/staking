SnapshotRewards_Integration_Concrete_Test
├── when delegate call
│  └── it should revert
└── when no delegate call
   ├── when null
   │  └── it should revert
   └── when not null
      ├── given staked amount zero
      │  └── it should revert
      └── given staked amount not zero
         ├── when end time in future
         │  └── it should take snapshot
         ├── when end time in present
         │  └── it should take snapshot
         └── when end time in past
            ├── it should update global rewards snapshot
            ├── it should update user rewards snapshot
            └── it should emit {SnapshotRewards} event
