CreatePool_Integration_Concrete_Test
├── when delegate call
│  └── it should revert
└── when no delegate call
   ├── when admin zero address
   │  └── it should revert
   └── when admin not zero address
      ├── when start time in past
      │  └── it should revert
      ├── when start time in present
      │  └── it should create the pool
      └── when start time in future
         ├── when end time less than start time
         │  └── it should revert
         ├── when end time equals start time
         │  └── it should revert
         └── when end time greater than start time
            ├── when staking token zero address
            │  └── it should revert
            └── when staking token not zero address
               ├── when reward token zero address
               │  └── it should revert
               └── when reward token not zero address
                  ├── when total rewards zero
                  │  └── it should revert
                  └── when total rewards not zero
                     ├── it should create the pool
                     ├── it should bump next pool id
                     ├── it should transfer total rewards amount to this contract
                     └── it should emit {Transfer} and {CreatePool} events
