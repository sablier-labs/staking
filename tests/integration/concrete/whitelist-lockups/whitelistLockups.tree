WhitelistLockups_Integration_Concrete_Test
├── when delegate call
│  └── it should revert
└── when no delegate call
   ├── when caller not comptroller
   │  └── it should revert
   └── when caller comptroller
      ├── given already whitelisted
      │  └── it should revert
      └── given not whitelisted
         ├── when lockup not implement required interface
         │  └── it should revert
         └── when lockup implements required interface
            ├── when isAllowedToHook returns false
            │  └── it should revert
            └── when isAllowedToHook returns true
               ├── it should emit {LockupWhitelisted} event
               └── it should whitelist lockup
