Getters_Integration_Concrete_Test::amountStakedByUser
├── when null
│  └── it should revert
└── when not null
   ├── when zero address
   │  └── it should revert
   └── when not zero address
      └── it should return correct amounts

Getters_Integration_Concrete_Test::getAdmin
├── when null
│  └── it should revert
└── when not null
   └── it should return the admin

Getters_Integration_Concrete_Test::getEndTime
├── when null
│  └── it should revert
└── when not null
   └── it should return the end time

Getters_Integration_Concrete_Test::getRewardToken
├── when null
│  └── it should revert
└── when not null
   └── it should return the reward token

Getters_Integration_Concrete_Test::getStakingToken
├── when null
│  └── it should revert
└── when not null
   └── it should return the staking token

Getters_Integration_Concrete_Test::getStartTime
├── when null
│  └── it should revert
└── when not null
   └── it should return the start time

Getters_Integration_Concrete_Test::getTotalRewards
├── when null
│  └── it should revert
└── when not null
   └── it should return the total rewards

Getters_Integration_Concrete_Test::globalSnapshot
├── when null
│  └── it should revert
└── when not null
   ├── when start time in future
   │  └── it should return correct return values
   ├── when start time in present
   │  └── it should return correct return values
   └── when start time in past
      ├── when end time in future
      │  └── it should return correct return values
      └── when end time not in future
         └── it should return correct return values

Getters_Integration_Concrete_Test::isLockupWhitelisted
├── when zero address
│  └── it should revert
└── when not zero address
   ├── given not whitelisted
   │  └── it should return false
   └── given whitelisted
      └── it should return true

Getters_Integration_Concrete_Test::stakedStream
├── when zero address
│  └── it should revert
└── when not zero address
   ├── given not whitelisted
   │  └── it should revert
   └── given whitelisted
      ├── when stream not staked
      │  └── it should revert
      └── when stream staked
         └── it should return campaign id and owner

Getters_Integration_Concrete_Test::totalAmountStaked
├── when null
│  └── it should revert
└── when not null
   └── it should return the total amount staked

Getters_Integration_Concrete_Test::userSnapshot
├── when null
│  └── it should revert
└── when not null
   ├── when zero address
   │  └── it should revert
   └── when not zero address
      ├── when start time in future
      │  └── it should return correct return values
      ├── when start time in present
      │  └── it should return correct return values
      └── when start time in past
         ├── when end time in future
         │  └── it should return correct return values
         └── when end time not in future
            └── it should return correct return values

Getters_Integration_Concrete_Test::wasCanceled
├── when null
│  └── it should revert
└── when not null
   ├── given not canceled
   │  └── it should return false
   └── given canceled
      └── it should return true