OnSablierLockupCancel_Integration_Concrete_Test
├── when delegate call
│  └── it should revert
└── when no delegate call
   ├── when caller not lockup
   │  └── it should do nothing
   └── when caller lockup
      ├── given lockup not whitelisted
      │  └── it should do nothing
      └── given lockup whitelisted
         ├── given stream not staked
         │  └── it should do nothing
         └── given stream staked
            ├── it should emit {SnapshotRewards} event
            ├── it should adjust global staked amount
            └── it should adjust user staked amount